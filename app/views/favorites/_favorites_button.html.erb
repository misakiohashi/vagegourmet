      
<% if current_user.favorite?(restaurant) %>
    <%= form_with(model: current_user.favorites.find_by(restaurant_id: restaurant.id), local: true, method: :delete) do |f| %>
    <%= hidden_field_tag :like_id, restaurant.id %>
    <%= f.submit '☆行きたい登録済み', class: 'btn btn-secondary btn-block' %>
    <% end %>
<% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
    <%= hidden_field_tag :like_id, restaurant.id %>
    <%= f.submit '☆行きたい', class: 'btn btn-warning btn-block' %>
    <% end %>
<% end %>
        